export class userAge{
  constructor (planet) {
    this.age = age;
    this.planet = planet;
  }
}

mercuryYears(){
  this.age = .24
  const userAge = Math.round(this.age / this.planet);
  return userAge;
  }
venusYears(){
  this.age = .62
  const userAge = Math.round(this.age / this.planet);
  return userAge;
  }
marsYears(){
  this.age = 1.88
  const userAge = Math.round(this.age / this.planet);
  return userAge;
  }
jupiterYears(){
  this.age = 11.86
  const userAge = Math.round(this.age / this.planet);
  return userAge;
  }
